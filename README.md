# My Local Config for tmux, zsh and nvim

#### installation

1. Clone this repo in the ~/dotfiles directory
2. use the stow . command in the ~/dotfiles directory
   - `sudo apt-get install stow`
3. finished
